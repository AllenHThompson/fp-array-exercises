//USE FILTER TO RETURN POSITIVE NUMBERS OUT OF //AN ARRAY
var num = [-1, 4, -3, 5, 10, -9];
var posNum = num.filter(function(num) {
     return num > 0;
});

//USE MAP AND RETURN AN ARRAY WITH EACH //NUMBER SQUARED
var num = [2, 4, 6, 9];
var squNum = num.map(function(num) {
     return element * element;
});
